# 用户决策

## 2026-08-29：建立材料知识整合场景 skill

| 项目 | 记录 |
| --- | --- |
| 状态 | 已废弃；由“2026-08-30：采用最终的知识模型整理提示”取代。 |
| 用户要求 | 建立场景 skill，把用户指定材料中实际讲解的全部知识整合进既有知识模型；不补入材料未讲解的背景知识或演绎结论，不核验事实；原始材料默认不保存；主要使用中文；形成完整拟议模型并取得用户确认后才能写入数据库。知识库结构按照通用数据结构维护，不为本场景设置局部修改范围、特殊节点身份或额外审批。 |
| 用户给出的原因 | 用户说明输入材料可能由教程和问答共同组成，也可能完全采用对话等其他形式，因此输入不能限定为教程。用户还说明知识结构应按照数据库及其数据结构的需要创建，不需要为本场景作特殊安排。其余要求未另行说明原因。 |
| 实际改动 | 新增 `integrate-explained-knowledge` 场景 skill，以 `SKILL.md` 规定材料边界、知识选择、推理边界、既有知识整合、原始材料处理、中文正文和确认后写入流程；不增加脚本、第二份 schema 或注册清单。 |

## 2026-08-30：移除工作范围表述

| 项目 | 记录 |
| --- | --- |
| 状态 | 当前。 |
| 用户要求 | 从场景 skill 中直接移除“你的工作范围是用户提供的内容，工作是将它们中的知识整理成知识模型”。 |
| 用户给出的原因 | 该表述使执行者把材料明确指认的一般知识错误地限制在材料中的实例主语上。 |
| 实际改动 | 删除该句，不增加替代表述。 |

## 2026-08-30：按照知识本身的结构组织知识模型

| 项目 | 记录 |
| --- | --- |
| 状态 | 当前。 |
| 用户要求 | 在场景 skill 中加入“按照知识本身的结构来组织知识模型，而不是按照教程中讲解的结构来组织知识模型”。 |
| 用户给出的原因 | 前一次测试的内容基本合格，只剩知识结构仍沿用教程讲解结构的问题。 |
| 实际改动 | 将用户给出的原句加入形成知识模型时的注意事项。 |

## 2026-08-30：采用最终的知识模型整理提示

| 项目 | 记录 |
| --- | --- |
| 状态 | 当前；由“2026-08-30：补充前置知识、标准抽象形式和变更确认”补充。 |
| 用户要求 | 采用用户直接修改后的知识模型整理提示。 |
| 用户给出的原因 | 用户确认该版本产生的知识模型符合预期，当前没有其他问题。 |
| 实际改动 | 将整理对象表述为教程中的知识；知识范围包含教程中明确被主题化的知识，以及能够看出但没有被主题化的知识；删除此前对子弹层级切分方式的额外规定。 |

## 2026-08-30：补充前置知识、标准抽象形式和变更确认

| 项目 | 记录 |
| --- | --- |
| 状态 | 当前。 |
| 用户要求 | 材料默认读者已经掌握的前置知识在知识库中缺失时也要整理；保留按照知识本身结构组织知识模型的要求；抽象形式必须采用已有的标准形式；修改数据库前向用户展示拟议变更并取得确认，不规定父路径展示格式。 |
| 用户给出的原因 | 前一次整理遗漏了教程依赖的前置知识，并把讲解材料保存为知识；数据库操作也不一定只是写入新节点。 |
| 实际改动 | 增加前置知识整理要求；保留按照知识本身结构组织的原有条款；限定标准抽象形式；将确认范围和数据库操作统一表述为变更。 |
