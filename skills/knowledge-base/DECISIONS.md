# 用户决策

## 2026-08-28：建立中文知识库通用 skill

| 项目 | 记录 |
| --- | --- |
| 用户要求 | 按 writing-great-skills 与 skill-creator 编写中文通用 skill，指导知识库数据的使用。FSRS 作为按需加载的子 skill，说明算法与各项参数。schema 直接放在 skill 中。 |
| 用户给出的原因 | 通用 skill 面向数据。schema 与 skill 保持单一权威来源，以避免双源。 |
| 实际改动 | 建立通用入口及 FSRS 子 skill；将原有 SQL 原样迁入本目录并更新项目路径约定；提供 FSRS-6 派生值的离线计算脚本。 |

## 2026-08-28：展开父子关系与兄弟顺序的说明

| 项目 | 记录 |
| --- | --- |
| 状态 | 已废弃；由“2026-08-30：使用子弹笔记解释知识记录的组织关系”取代。 |
| 用户要求 | 单独说明父子关系与兄弟顺序，使用 Markdown 文章和概念叙述的具体结构展示组织方式。 |
| 用户给出的原因 | 父子关系是宽泛且灵活的组织关系，兄弟关系类似前后自然段。实际整理应依据内容，不必对照“语义层级”等固定定义。 |
| 实际改动 | 将原表格中的两行改为独立说明，并加入标题层级与正文共同排列的文章实例，以及按叙述需要展开概念的实例。 |

## 2026-08-30：使用子弹笔记解释知识记录的组织关系

| 项目 | 记录 |
| --- | --- |
| 状态 | 当前。 |
| 用户要求 | 数据库中的父子节点和兄弟节点对应子弹笔记中的父子 bullet 和同级 bullet。 |
| 用户给出的原因 | 子弹笔记已经表达知识语义之间的层级与并列关系，数据库结构应当保存相同关系。 |
| 实际改动 | 每个 bullet 对应一条知识记录；直接缩进对应 `parent_id`，同级 bullet 共享 `parent_id`，`sibling_order` 保存同级 bullet 的顺序。 |

## 2026-08-28：采用 py-fsrs 并保存完整复习历史

| 项目 | 记录 |
| --- | --- |
| 用户要求 | 先提交现有内容；把 py-fsrs 作为随 skill 提供的脚本实现，并说明用法；变更 schema，使数据库自动记录复习历史。允许加入第三方代码。 |
| 用户给出的原因 | 需要支持完整 FSRS，包括其参数优化能力。 |
| 实际改动 | 随 skill 保存固定版本的 py-fsrs 源码及许可；计算脚本调用原生实现；schema 保存完整状态与调度配置，并提供自动追加历史的事务入口；参数优化与状态重算使用数据库导出的原生日志。 |

## 2026-08-29：分开数据定义、FSRS 计算与数据库读写

| 项目 | 记录 |
| --- | --- |
| 用户要求 | schema 保存数据定义。Python 脚本提供 FSRS 计算服务；agent 使用独立的数据库接口完成读写。 |
| 用户给出的原因 | agent 可能分别拥有 Python 工具与数据库接口，但 Python 执行环境无法调用数据库接口。 |
| 实际改动 | 移除 schema 中的数据操作函数；保持 Python 脚本接收和返回 JSON；将普通 SQL 保存为 skill 的查询资料，并说明由 agent 通过数据库接口执行。 |

## 2026-08-29：个人知识库默认拒绝 Data API 访问

| 项目 | 记录 |
| --- | --- |
| 用户要求 | 将当前 schema 部署到个人使用的 Supabase 项目。 |
| 用户给出的原因 | 该知识库只供个人使用。 |
| 实际改动 | 为全部知识库表启用 RLS，不创建 `anon` 或 `authenticated` 策略；将 `effective_record_tag` 设为 `security_invoker`，使视图遵守底层表的 RLS。已授权的 SQL 通道与 `service_role` 保持可用。 |

## 2026-08-30：共享 Scheduler 配置

| 项目 | 记录 |
| --- | --- |
| 用户要求 | 建立 `scheduler_config` 一对多 `fsrs` 的关系。 |
| 用户给出的原因 | Scheduler 配置应由多个 FSRS 对象共同引用。 |
| 实际改动 | `scheduler_config` 单独保存原生 Scheduler JSON，`fsrs.scheduler_config_id` 以外键引用配置；创建与重算查询按配置 id 建立或改变关联。 |

## 2026-08-30：记录行级变更历史

| 项目 | 记录 |
| --- | --- |
| 用户要求 | 自动保存模型通过 Supabase 插件提交的数据库变更，以便在错误操作后恢复数据。 |
| 用户给出的原因 | Supabase 插件直接以数据库管理角色执行 SQL，Data API 权限不能限制模型；需要使用低工程成本的统一机制记录普通数据变更。 |
| 实际改动 | 使用独立的 `audit.row_change` 保存七张持久化业务表的行级变更；一个通用 trigger 函数记录事务、操作以及变更前后快照。行级历史用于恢复普通 DML，项目外数据库导出负责管理角色能够破坏历史的情况。 |
