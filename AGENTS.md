# 项目约定

项目文档、代码注释和开发交流使用中文。数据库标识符、文件名以及已有标准术语保留其通用写法。

## 工程目标

### 项目做什么

本项目把个人知识管理从“人维护笔记”转变为“人进行认知活动，AI 维护认知活动使用和产生的数据”。人的核心工作是学习和思考；LLM 负责读取、整理和持续维护知识，并为当前任务临时生成人类可用的表现形式。

系统由三部分组成：以 PostgreSQL 和 Supabase 承载的核心知识库、适用于整个知识库的通用 skill、面向学习、间隔重复和深入思考等认知活动的场景 skill。操作知识库所需的工具属于使用它们的 skill。

知识库直接保存自然语言文本。原始教材、教程以及认知活动产生的整理结果都可以成为知识内容。数据库只额外保存必须稳定存在的数据管理结构，使 LLM 能够定位上下文、探索相关内容并维护知识。

### 什么结果算正确

- 用户进行学习、复习或思考时，场景 skill 能够从同一个知识库取得相关内容，并把新获得或新创造的知识写回该知识库。
- LLM 能够通过有序森林、记录级引用和可继承标签定位与扩展上下文，而不要求数据库理解正文的自然语言语义。
- 正在形成的内容与已经稳定的知识使用同一种知识记录；用途差异可以由树上继承的标签表达。
- 面向人的教程、复习问题、思考摘要和其他界面按当前任务生成，不成为需要用户持续维护的核心数据对象。
- FSRS v6 调度对象拥有独立的记忆状态，并通过关联取得复习所需的一条或多条知识记录。知识记录的存储边界不决定 FSRS 对象的粒度。
- 新增认知活动时，优先增加场景 skill，不因场景特有的语义或交互方式修改核心 schema。

### 容易误解的目标边界

- `knowledge_record` 是连续文本的存储和组织单位，不是命题、语义单元、精确引用单位或 FSRS 单位。
- 数据库 schema 不区分来源材料、用户已经学到的知识和用户创造的私知识。这些区别由自然语言内容、知识组织方式和具体 skill 解释。
- 树中的父子关系表达语义层级，相邻兄弟关系表达同层文本的先后。数据库只保存这些结构关系，不把父子关系继续编码为具体的关系类型。
- 存储边界只服务于数据管理，不承担正文的语义切分。引用只需帮助 LLM 拉起相关知识，正文中的具体相关部分由 LLM 在读取后判断。
- 传统笔记、教程和摘要是按需生成或保存的知识表现形式，不是系统围绕其建模的核心对象。
- 来源的组织方式、知识冲突的处理方式，以及原始交互、预览 diff、批注和备份等内容是否保存及如何保存，均由相关场景 skill 决定，不属于核心知识模型。
- FSRS 对象与知识记录是多对多关系。不得把一条知识记录默认解释为一张复习卡，也不得要求一个 FSRS 对象只使用一条知识记录。

## 项目结构

| 路径 | 职责 |
| --- | --- |
| `AGENTS.md` | 记录工程目标、稳定概念、目录职责和扩展边界。 |
| `supabase/schemas/knowledge_base.sql` | 核心知识模型的权威声明式 schema；定义知识记录、记录级引用、可继承标签、FSRS 状态及二者关联。 |
| `tools/` | 开发、检查和维护本仓库时使用的辅助工具。认知活动中操作知识库的工具不放在这里。 |
| `tools/knowledge-viewer/` | 本地只读知识库查看器。`run` 是启动入口，`server.py` 通过固定查询读取 Supabase 当前快照，`public/` 展示有序森林、有效标签、记录级引用和 FSRS 关联，并提供搜索与定位能力。该工具不提供知识库写入接口，也不属于认知活动中操作知识库的工具。 |
| `skills/knowledge-base/` | 整个知识库的通用 skill 所在目录。建立实现时以 `SKILL.md` 作为入口；该 skill 使用的知识库操作工具也放在此目录中。 |
| `skills/scenarios/` | 各类认知活动的场景 skill 所在目录。每个场景使用独立子目录，并在自己的目录中保存该场景使用的知识库操作工具。 |

除非已有实现需要，不预先增加应用框架、通用注册框架或第二份 schema 表示。

## 项目概念

| 术语 | 含义 |
| --- | --- |
| 知识内容 | 可供后续认知活动读取和使用的自然语言文本，包括来源材料和认知活动产生的内容。 |
| 知识记录（knowledge record） | 保存一份完整连续文本、具有稳定身份并占据有序森林中一个位置的记录。它通常承载与一个自然段相近的信息量；这是组织时采用的典型粒度，不是数据库强制的语义单位。 |
| 当前知识模型 | 当前有效的知识记录及其组织关系。历史版本、diff、批注和原始交互不因其历史身份自动成为当前知识模型的一部分。 |
| 有序森林 | 由 `parent_id`、`depth` 和 `sibling_order` 表示的知识记录层级。它提供无环的父子结构和确定的同级顺序。 |
| 相邻兄弟节点 | 拥有同一父节点并在 `sibling_order` 中前后相邻的两条知识记录。它们对应处于同一语义层级、在连续文本中前后相邻的自然段。 |
| 父子节点 | 表达知识内容的语义层级。父节点与子节点可以分别承担抽象与具体、宏观与微观、综述与分述、标题与内容的作用；数据库不保存这些具体关系类型。 |
| 记录级引用（knowledge reference） | 从一条知识记录指向另一条知识记录的有向关系，用于帮助 LLM 取得相关上下文；不保存精确位置或关系类型。 |
| 直接标签 | 直接附着在一条知识记录上的标签。 |
| 有效标签 | 一条记录直接拥有或从祖先继承的标签。数据库只持久化直接标签。 |
| working 内容 | 被 `working` 标签直接或继承标记的知识记录子树；它不是独立实体类型。 |
| 私知识 | 用户在思考中形成或创造的知识内容。它仍使用普通知识记录保存。 |
| FSRS 对象 | 一个独立的 FSRS v6 间隔重复调度对象，保存当前 Stability、Difficulty、最后复习时间和下次到期时间。 |
| FSRS 关联 | FSRS 对象与其复习时需要取用的知识记录之间的多对多关系。每个 FSRS 对象至少关联一条知识记录。 |
| 知识库通用 skill | 规定 LLM 如何使用整个知识库的共同能力；它不替代场景 skill 对具体认知活动的安排。 |
| 场景 skill | 面向一种认知活动的完整处理方式，例如学习、间隔重复或深入思考。场景特有的组织、交互和历史处理由它决定。 |
| 人类界面 | LLM 针对当前任务生成的教程、问题、反馈、摘要或其他表现形式。它不是核心存储模型。 |

## 横向扩展点

### 场景 skill

场景 skill 是当前唯一确定的同类实现扩展点。其抽象是一种认知活动的处理方式：通过知识库通用 skill 和相关 skill 目录中的工具读取已有知识，执行该场景的交互与判断，并按需维护知识库。

- 代码入口：`skills/scenarios/<场景名>/SKILL.md`
- 注册位置：`skills/scenarios/`。存在包含有效 `SKILL.md` 的场景子目录即表示该场景已经注册，不维护第二份注册清单。
- 扩展边界：场景特有的内容组织、交互产物、处理策略和操作工具保留在该场景目录中；只有多个场景共同需要且属于知识库一般操作的能力，才进入知识库通用 skill 及其目录。

核心 schema 和知识库通用 skill 均保持单一权威实现。标签名称和树的组织约定属于数据与 skill 的开放用法，不另设代码注册机制。
